// Type: identity
double *hidden_activation(double v[], int size) {
    double *result = malloc(sizeof(double) * size);
    int i;
    for (i = 0; i < size; i++) {
        result[i] = v[i];
    }
    return result;
}